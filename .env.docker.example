# Copy this to `.env` on the server (next to docker-compose.yml) and fill in.
# `.env` is gitignored — never commit real secrets.

# --- Domain / HTTPS ---
DOMAIN=ss.alif24.uz
LETSENCRYPT_EMAIL=siz@example.com
APP_URL=https://ss.alif24.uz

# --- PostgreSQL (compose builds DATABASE_URL from these) ---
POSTGRES_USER=ishla
POSTGRES_PASSWORD=KUCHLI_PAROL_shu_yerda
POSTGRES_DB=ishla_db

# --- Session ---
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
AUTH_SECRET=

# --- Azure OpenAI (CV tahlili, AI mijoz, baholash) ---
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=

# --- Azure Speech (ovoz) ---
AZURE_TTS_API_KEY=
AZURE_TTS_REGION=

# --- Google login (ixtiyoriy) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Telegram login (ixtiyoriy) ---
TELEGRAM_BOT_TOKEN=
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=
